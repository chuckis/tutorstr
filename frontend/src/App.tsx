import "./App.css";

export default function App() {
  return (
    <main className="app">
      <section className="card">
        <h1>Tutorstr</h1>
        <p>Frontend bootstrapped with React, TypeScript, Vite, and PWA basics.</p>
        <p>Next: build the tutor discovery and booking flows.</p>
      </section>
    </main>
  );
}
